'use strict';

import { Main } from '../components';

export function One(): JSX.Element {
    return (
        <Main description="1ページ目です。" title="1">
            <p>1ページ目です。</p>
        </Main>
    );
}
